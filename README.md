After knitting and rendering `inputs_Monitor.Rmd`, overwrite this file with a symlink to `inputs_Monitor.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/michelet/Documents/GitHub/inputs_Monitor
rm README.md && ln -s inputs_Monitor.md README.md
```
